<h1>Listing All Projects</h1>
<br />
<table id="project_table">
  <thead><tr>
    <th>Title</th>
    <th>Entries</th>
    <th>Budget</th>
    <th>Time Remaining</th>
  </tr></thead>

  <% @projects.each do |project| %>
  <tr>
    <td>
      <% if project.status == "Rewarded" %>
      <img src="/images/rewards_icon.png" height="32" width="24" />
      <% end %>
      <h3><%=link_to project.title,project_path(project) %></h3><p><%= project.description.truncate(150) %></p></td>
      <td><%= project.submissions.size%></td>
      <td><%= project.budget %></td>
      <td>
        <% if project.status == "open" %>
        <%= distance_of_time_in_words(project.deadline,Time.now) %>
        <% else %>
        <%= project.status %>
        <% end %>
      </td>
    </tr>
    <% end %>
  </table>

  <br />


